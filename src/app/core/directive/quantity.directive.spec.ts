import { QuantityDirective } from './quantity.directive';

describe('QuantityDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityDirective();
    expect(directive).toBeTruthy();
  });
});
